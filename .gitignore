# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# scratch
*.log
tmp/

# stray local artefacts
cp*/
# Screenshots I write while checking renders land in the repo root.
# Must not be "*.png" — that would swallow the site's own images.
/*.png

# local tooling
.claude/
src/live/
src/mirror/

# npm (the Anthropic SDK; Cloudflare bundles it into the Function at deploy)
node_modules/

translation-todo.json
translation-review-required.json
